import { FilterInternalPipe } from './filter-internal.pipe';

describe('FilterInternalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInternalPipe();
    expect(pipe).toBeTruthy();
  });
});

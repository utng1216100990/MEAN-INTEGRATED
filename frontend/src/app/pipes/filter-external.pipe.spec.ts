import { FilterExternalPipe } from './filter-external.pipe';

describe('FilterExternalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExternalPipe();
    expect(pipe).toBeTruthy();
  });
});